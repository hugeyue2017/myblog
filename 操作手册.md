# 操作手册（一页纸）

> 这个站是 Hugo + PaperMod，代码托管在 GitHub，Vercel 自动构建，域名 https://www.minipiggy.top/
> 你平时只会做四件事：写文章、放照片、改样式、发布。下面按顺序讲。

## 记住三句话

| 东西 | 放哪 |
| --- | --- |
| 文章 | `content/posts/` |
| 照片原图 | `assets/images/` |
| 样式（颜色/字号/间距） | `assets/css/extended/warm-editorial.css` 一个文件 |

**图别放 `static/`**。放那儿 Hugo 不会处理它——原图多大，访客就下多大（之前首页那张 11.6MB 就是这么来的）。
放 `assets/images/` 才会自动压缩。

---

## 一、写一篇新文章

**第 1 步**：在 `content/posts/` 下新建文件，文件名建议 `2026-10-01-first-smile.md`（日期 + 英文短标题，中间别用空格）。

**第 2 步**：把下面这块粘到文件开头，改成你的内容：

```yaml
---
title: "你第一次笑"
date: 2026-10-01
draft: false
images: ['images/first-smile.jpg']
---
```

- `title` 显示在标题和浏览器标签上。
- `date` 决定它在首页时间轴上的位置，也是排序依据（新的在最上面）。
- `images` 是分享到微信/朋友圈时的缩略图；**不写就用站点默认那张**，不会出错。
- `draft: true` 表示草稿，不会出现在线上。

**第 3 步**：写正文，段落之间空一行。结尾想留落款就加：

```html
<p class="sig">永远爱你的爸爸（胖虎） 妈妈（猪哥）</p>
<p class="sig-date">2026年10月</p>
```

这两行会渲染成右对齐的衬线体落款，跟现在的文章一样。

---

## 二、放照片 / 换照片

**第 1 步**：手机导出的原图直接拖进 `assets/images/`，命名用英文小写加连字符，例如 `first-smile.jpg`。
（中文名、空格、大写混用容易在链接上出岔子，忍一下。）

**第 2 步**：在正文里写一行 Markdown：

```markdown
![你第一次笑](images/first-smile.jpg)
```

就这两步。构建时会自动完成下面这些事，你不用管：

- 压缩体积、**抹掉 EXIF**（含 GPS 定位和拍摄时间）
- 生成 WebP 版本（比 JPEG 再小 30% 左右）
- 生成 480 / 800 / 1200 / 1800 四档尺寸，手机只下最小那档
- 预留好宽高，图片加载时页面不会跳动
- 首页时间轴自动取这张图当封面

**换图**就是同名覆盖：新图改名成旧图的名字，拖进去替换即可。**删图**要连着正文里那行 Markdown 一起删，不然会出现裂图。

---

## 三、发布

**第 1 步**：提交

```bash
git add .
git commit -m "新增：你第一次笑"
```

**第 2 步**：推送

```bash
git push
```

推送后 Vercel 自动开始构建，**大约 1 分钟**后线上生效。去 https://www.minipiggy.top/ 刷新看看就行。

> 刚推完发现写错了？改完再 `add` / `commit` / `push` 一次即可，不用慌。

---

## 四、本地预览（想先看看效果）

装好 Hugo 0.146.0 **extended** 版后，在仓库根目录：

```bash
hugo server -D
```

浏览器打开 http://localhost:1313/ 。改文件会实时刷新，`-D` 表示连草稿一起显示。

---

## 五、改样式：改哪儿

所有视觉都收在 `assets/css/extended/warm-editorial.css` 里，**不要动 `themes/PaperMod/`**（那是主题本体，改了以后升级主题会被覆盖）。

文件开头有一组变量，改这几个就够了：

```css
--clay: #b4552d;      /* 全站唯一的强调色：陶土红 */
--paper: #faf7f2;     /* 浅色模式底色（米白） */
--ink: #1b1a18;       /* 正文墨色 */
--gap: 46px;          /* 段落间距，想更疏就调大 */
```

深色模式的对应值在文件后半段 `[data-theme="dark"]` 那块里，改的时候两边一起改。

站点名、导航菜单、描述文字在 `hugo.toml`，里面每一段都有中文注释说明为什么这么写。

---

## 六、常见问题

**推上去了线上没变？**
先去 Vercel 看构建日志。多半是 Markdown 语法或 front matter 缩进写错了，日志里会直说哪一行。

**图片显示不出来？**
九成是路径写错。正文里必须写 `images/xxx.jpg`（**不带前面的斜杠**），文件名大小写要完全一致。

**想加一个新页面（比如「相册」）？**
在 `content/` 下新建 `albums.md`，照抄 `content/about.md` 的开头即可，然后去 `hugo.toml` 的 `[[menu.main]]` 里加一条导航。
